services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vocal
      POSTGRES_PASSWORD: vocal
      POSTGRES_DB: vocal
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
